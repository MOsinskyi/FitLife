services:
  redis:
    image: redis:8.4-alpine
    container_name: redis_container
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - cache:/data

volumes:
  cache:
    driver: local
